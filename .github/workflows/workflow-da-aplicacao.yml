name: Deploy da Aplicacao e Testes Cypress

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código da aplicação
        uses: actions/checkout@v3

      - name: Simular Deploy da Aplicação
        run: echo "Aplicação implantada com sucesso!"

      - name: Disparar casos de testes do repositório de testes (Cypress)
        id: trigger-tests
        uses: peter-evans/repository-dispatch@v2
        with:
          repository: viniciuscarneironascimento/mark85-cypress-api-papito
          event-type: run-cypress-tests
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN_APLICACAO }}

      - name: Aguardar execução da pipeline de testes
        id: wait-tests
        run: |
          echo "Aguardando execução da pipeline de testes..."
          STATUS_URL="https://api.github.com/repos/viniciuscarneironascimento/mark85-cypress-api-papito/actions/runs"
          MAX_ATTEMPTS=30
          ATTEMPT=0
          RUN_ID=""
          
          while [ $ATTEMPT -lt $MAX_ATTEMPTS ]; do
            sleep 30
            RESPONSE=$(curl -s -H "Authorization: token ${{ secrets.PERSONAL_ACCESS_TOKEN_APLICACAO }}" $STATUS_URL)
            RUN_ID=$(echo "$RESPONSE" | jq -r '.workflow_runs[] | select(.name == "Executar testes Cypress") | .id' | head -n 1)
            LATEST_RUN_STATUS=$(echo "$RESPONSE" | jq -r '.workflow_runs[] | select(.id == '"$RUN_ID"') | .conclusion' | head -n 1)

            echo "Tentativa $((ATTEMPT+1)): Status atual - $LATEST_RUN_STATUS"

            if [[ "$LATEST_RUN_STATUS" == "success" ]]; then
              echo "Pipeline de testes finalizada com sucesso!"
              echo "RUN_ID=$RUN_ID" >> $GITHUB_ENV
              break
            elif [[ "$LATEST_RUN_STATUS" == "failure" || "$LATEST_RUN_STATUS" == "cancelled" ]]; then
              echo "A pipeline de testes no Cypress falhou. Interrompendo o deploy!"
              exit 1
            fi

            ATTEMPT=$((ATTEMPT+1))
          done

          if [[ "$ATTEMPT" -eq "$MAX_ATTEMPTS" ]]; then
            echo "Tempo limite excedido esperando a pipeline de testes. Interrompendo o deploy!"
            exit 1
          fi

      - name: Obter link do job de testes Cypress
        id: get-test-job-link
        run: |
          JOBS_URL="https://api.github.com/repos/viniciuscarneironascimento/mark85-cypress-api-papito/actions/runs/${{ env.RUN_ID }}/jobs"
          RESPONSE=$(curl -s -H "Authorization: token ${{ secrets.PERSONAL_ACCESS_TOKEN_APLICACAO }}" $JOBS_URL)
          
          JOB_ID=$(echo "$RESPONSE" | jq -r '.jobs[0].id')
          
          if [[ -z "$JOB_ID" || "$JOB_ID" == "null" ]]; then
            echo "ERRO: Job ID não encontrado para a execução RUN_ID=$RUN_ID"
            exit 1
          fi
          
          JOB_URL="https://github.com/viniciuscarneironascimento/mark85-cypress-api-papito/actions/runs/${{ env.RUN_ID }}/job/${JOB_ID}"
          echo "Job de testes: $JOB_URL"
          echo "TEST_JOB_URL=$JOB_URL" >> $GITHUB_ENV

      - name: Exibir link do job de testes
        run: echo "Acesse o job dos testes Cypress: ${{ env.TEST_JOB_URL }}"

      - name: Deploy Final
        run: echo "Deploy finalizado com sucesso!"

      - name: Baixar relatórios do Mochawesome
        uses: dawidd6/action-download-artifact@v2
        with:
          github_token: ${{ secrets.PERSONAL_ACCESS_TOKEN_TEST_CYPRESS }}
          repo: viniciuscarneironascimento/mark85-cypress-api-papito
          workflow: workflow-do-cypress.yml
          name: cypress-mochawesome-report
          path: mochawesome-reports
          run_id: ${{ env.RUN_ID }}
          search_artifacts: true

      - name: Baixar link do Cypress Cloud
        uses: dawidd6/action-download-artifact@v2
        with:
          github_token: ${{ secrets.PERSONAL_ACCESS_TOKEN_APLICACAO }}
          repo: viniciuscarneironascimento/mark85-cypress-api-papito
          workflow: workflow-do-cypress.yml
          name: cypress-cloud-url
          path: ./
          run_id: ${{ env.RUN_ID }}
          search_artifacts: true

      - name: Exibir link do Cypress Cloud
        run: |
          if [ -f cypress-cloud-url.txt ]; then
            cat cypress-cloud-url.txt
          else
            echo "Arquivo cypress-cloud-url.txt não encontrado!"
            exit 1
          fi